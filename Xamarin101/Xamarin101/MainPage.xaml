<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
              xmlns:controls="clr-namespace:Xamarin101.Controls"
             xmlns:viewmodels="clr-namespace:Xamarin101.ViewModels"
             x:Class="Xamarin101.MainPage">

    <StackLayout>
        <ContentPage.BindingContext>
            <viewmodels:LoginViewModel />
        </ContentPage.BindingContext>

            <controls:FacebookLoginButton
            OnSuccess="{Binding OnFacebookLoginSuccessCmd}"
            OnError="{Binding OnFacebookLoginErrorCmd}"
            OnCancel="{Binding OnFacebookLoginCancelCmd}" />
    </StackLayout>

</ContentPage>
